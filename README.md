# movie_tracker

Repository layout:

- `public/` contains the static website that GitHub Pages serves.
- `public/media/` is where you can drop the site icon or other images.

If you replace the favicon, keep the file name in `public/index.html` aligned with the asset you add to `public/media/`.
